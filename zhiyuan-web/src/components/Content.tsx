// components/Content.tsx
import React from "react";

/** 主内容区（默认占位） */
const Content: React.FC = () => (
  <div>
    <h1 className="text-2xl font-semibold mb-4">欢迎使用主从视图布局！</h1>
    <p>请选择左侧菜单以查看不同内容。</p>
  </div>
);

export default Content;
